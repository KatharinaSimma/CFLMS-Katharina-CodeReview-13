<nav class="navbar navbar-expand-lg navbar-light bg-white py-4">
  <a class="navbar-brand text-info" href="{{ path('event_index') }}"><i class="fas fa-play mr-3"></i>Big Events</a>
  <button class="navbar-toggler text-info" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse text-info" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link text-info" href="{{ path('event_index') }}">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-info" href="{{ path('event_new') }}">Create</a>
      </li>
    </ul>
  </div>
</nav>
